export enum Base {
  PRIMARY = "border-primary-700 bg-primary-700",
}

export enum Hover {
  PRIMARY = "hover:border-primary-800 hover:bg-primary-800",
}

export enum Focus {
  PRIMARY = "focus:border-primary-800 focus:bg-primary-800 focus:ring focus:ring-primary-400 focus:ring-opacity-50",
}

export enum Active {
  PRIMARY = "active:border-primary-800 active:bg-primary-800",
}
